<!-- DevExpress LoginView.xaml - Ogur Sentinel -->
<Page x:Class="Ogur.Sentinel.Devexpress.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
      xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
      xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
      Background="#1e1e1e">
    
    <Grid Margin="20">
        <dxlc:LayoutControl Orientation="Vertical" 
                           MaxWidth="350"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center">
            
            <!-- Title with Icon -->
            <dxlc:LayoutItem>
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20">
                    <TextBlock Text="🎮" 
                              FontSize="32"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"/>
                    <TextBlock Text="Ogur Sentinel" 
                              FontSize="28" 
                              FontWeight="Bold"
                              Foreground="White"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </dxlc:LayoutItem>
            
            <!-- Username -->
            <dxlc:LayoutItem Label="Username" 
                            LabelPosition="Top"
                            Margin="0,0,0,15">
                <dxlc:LayoutItem.LabelTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                  Foreground="White" 
                                  FontSize="14"
                                  Margin="0,0,0,5"/>
                    </DataTemplate>
                </dxlc:LayoutItem.LabelTemplate>
                <dxe:TextEdit x:Name="UsernameBox"
                             EditValue="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="14"
                             Height="36"
                             Background="#2d2d30"
                             Foreground="White"
                             BorderBrush="#3e3e42"
                             BorderThickness="1"/>
            </dxlc:LayoutItem>
            
            <!-- Password -->
            <dxlc:LayoutItem Label="Password" 
                            LabelPosition="Top"
                            Margin="0,0,0,15">
                <dxlc:LayoutItem.LabelTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" 
                                  Foreground="White" 
                                  FontSize="14"
                                  Margin="0,0,0,5"/>
                    </DataTemplate>
                </dxlc:LayoutItem.LabelTemplate>
                <dxe:PasswordBoxEdit x:Name="PasswordBox"
                                    EditValue="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    FontSize="14"
                                    Height="36"
                                    Background="#2d2d30"
                                    Foreground="White"
                                    BorderBrush="#3e3e42"
                                    BorderThickness="1"
                                    KeyDown="PasswordBox_KeyDown"/>
            </dxlc:LayoutItem>
            
            <!-- Remember Options -->
            <dxlc:LayoutItem Margin="0,0,0,20">
                <Border Background="#252526" 
                       BorderBrush="#3e3e42" 
                       BorderThickness="1" 
                       CornerRadius="6"
                       Padding="12">
                    <StackPanel>
                        <TextBlock Text="Zapamiętaj:" 
                                  Foreground="#999999" 
                                  FontSize="11"
                                  Margin="0,0,0,8"/>
                        
                        <RadioButton Content="Nic nie zapamiętuj"
                                    IsChecked="{Binding RememberNothing, Mode=TwoWay}"
                                    Foreground="White"
                                    FontSize="13"
                                    Margin="0,0,0,6"/>
                        
                        <RadioButton Content="Tylko login"
                                    IsChecked="{Binding RememberUsername, Mode=TwoWay}"
                                    Foreground="White"
                                    FontSize="13"
                                    Margin="0,0,0,6"/>
                        
                        <RadioButton Content="Login i hasło"
                                    IsChecked="{Binding RememberBoth, Mode=TwoWay}"
                                    Foreground="White"
                                    FontSize="13"/>
                    </StackPanel>
                </Border>
            </dxlc:LayoutItem>
            
            <!-- Error Message -->
            <dxlc:LayoutItem>
                <TextBlock Text="{Binding ErrorText}"
                          Foreground="{Binding ErrorForeground}"
                          Visibility="{Binding ErrorVisibility}"
                          FontSize="12"
                          TextWrapping="Wrap"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,0"/>
            </dxlc:LayoutItem>
            
            <!-- Login Button -->
            <dxlc:LayoutItem>
                <dx:SimpleButton Content="Login"
                                Command="{Binding LoginCommand}"
                                Style="{StaticResource PrimaryButton}"
                                Height="40"
                                FontSize="16"/>
            </dxlc:LayoutItem>
            
        </dxlc:LayoutControl>
    </Grid>
</Page>
